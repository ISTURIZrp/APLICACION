import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function ConfiguracionPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Configuración</CardTitle>
        <CardDescription>Aquí podrás realizar configuraciones avanzadas del sistema.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>El contenido para la configuración estará disponible aquí.</p>
      </CardContent>
    </Card>
  )
}
